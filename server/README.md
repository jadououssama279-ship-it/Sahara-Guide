# Sahara Guide Backend

This folder contains the source code for the Node.js/Express backend API.

## Getting Started
- Install dependencies with `npm install` or `yarn install`
- Start the development server with `npm start` or `node index.js`
